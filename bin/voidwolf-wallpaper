#!/bin/sh
# Wallpaper picker stub (PR8).
printf '%s\n' "voidwolf-wallpaper: not implemented yet (PR8)" >&2
command -v notify-send >/dev/null 2>&1 && notify-send "voidwolf" "Wallpaper picker lands in PR8"
exit 0
